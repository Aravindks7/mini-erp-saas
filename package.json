{
  "name": "erp-saas-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "check-all": "pnpm lint && pnpm type-check && pnpm test",
    "security-check": "pnpm audit",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "e2e": "playwright test",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "lint-staged": {
    "*.{js,ts,tsx,jsx,json,css,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
